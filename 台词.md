# 个人简介

评委老师好，我是刘明皓，首先说一下我的基本情况：我现在就读于重庆大学计算机专业，目前研二，曾经获得过重庆大学研一的二等奖学金、获得过第十三界蓝桥杯java B组国二等奖，第十五届中国大学生计算机设计大赛国三等奖，以及各类省奖。

# 然后是实习经历。

## 第一段实习经历是在住建委信息中心

这是一个事业单位，这段不是开发工作，所以简要的说，就是以甲方视角，配合电信等央国企，主要参与了规建运治一体化的数字孪生项目，了解了一个项目从诞生到结束的全流程，包括政策下发，需求侧提出功能点，业主是基于什么原因提出的功能需求，产品经理和开发团队又是怎么接下功能的，怎么开发的，以及最后测试和交付的过程是怎么样的，我都有了解。所以我在开发时面对新的功能需求，不停变化的功能需求时，会用多角度的方式来看待一些功能需求的合理性，从大局上思考自己的工作未来该怎么做，这么做会带来什么效益，领导真正想要什么，客户想要什么，而不只是局限于敲几行代码。

## 第二段实习经历是小米做测开。

协助车端测试管理平台和需求管理平台的测试工作。根据测试样例，完成自动生成脚本AI智能体。完成普通pytest测试脚本，并通过了解CI/CD（Jenkins/GitLab CI）流水线部署该脚本，完成流水线日常的报错管理修复；协助完成日常飞书工具的开发；使用JMeter完成压测、稳定性测试、基准测试等任务。在这段实习中接触了大量全栈和智能体的内容，需要横跨前端、后端、运维、产品。
- Playwright 自动化（前端知识）
- HTML/CSS 选择器（前端）
- Python + pytest（后端）
- Excel/Pandas 数据处理（数据）
- Docker 镜像构建（运维）
- CI/CD 流水线（DevOps）
- Allure 报告（工具链）
- 业务逻辑理解（产品）

# 然后是项目经历。

## 第1个项目是在小米实习时完成的AI智能体。
### 项目背景
在去年，我们组为测试内部工具网站UDE-R的UI正确性，编写了自动化脚本的流水线，此流水线上的脚本和新增脚本全部为手写，效率太低，且由于UI自动化的繁复性，工作量大，产出低，所以需要让AI来帮助解决这个问题。由于我是实习生里面先到的，而且指导老师的想法也是刚成形，所以由我来搭建初步的智能体架构。

### 关于智能体内两个最关键的组件
1. 由于mimo大模型是小米内部自研的模型，所以在公司内部是免token，于是选择的模型是mimov2.5。
2. 常规情况下，用截图会导致token消耗量大，把html直接喂给ai，会导致上下文过长 容易稀释注意力 导致幻觉，agent-browser 的snapshot命令通过"语义压缩 + ref 映射"，用最少的 token 把网页变成 LLM 最容易读懂、最不会产生幻觉、最能精确执行的结构化清单（但也为后面的失败埋下隐患，它会丢失部分层次结构，也不能很好的识别无文字无特定类选择器的图标）。 
3. 围绕这两个必用的东西，我搭建了调用他们的langchain，langgraph，Rag和少量的MCP。

### 你与AI的分工（主要做法）
1. 首先明确AI的边界：不是大模型不够强而是概率的叠加会非常不稳定。凡是存在唯一正确答案的环节，都由确定性代码处理，而非交给AI，典型的比如我们读取excel，截图操作，trace view和视频的录制，allure报告的添加，脚本的报错收集等等都是交给确定性代码来做。
2. 
2. 给AI补名词的近义词，解决"叫法"(元数据)：投入产出比最高的一件事，是所有优化手段里成本最低的。不需要改架构，不需要改算法，不需要改数据。对人来说显然是同一个东西，对 AI 来说就是两个完全不同的词，因为AI不一定理解业务中的细小区别。比如样例里面"项目详情"这个词，它同时是"项目编辑页面"和"项目展板"
3. 补名词的解释，解决"边界"(元数据)：不是解释"这个字段是什么"，而是告诉 AI"什么时候该用它、什么时候不该用"，要明确写出 禁止 的条目。比如"非正式属性"是用户自己的自定义属性，只在特定项目中使用，禁止在其他项目中使用，这里把具体的项目给ai，哪些项目卡片只能用哪些非正式属性。
4. 补动词的解释即业务规则，解决"路由"(元数据)："点击目标空间名称展开"中的"展开"这一个词，在-R网站中不同样例语境下有不同的意思，当样例要求进入项目列表的时候，这个词是"直接点击卡片本身"的同义词；当样例要配置空间卡片的时候，这个词的同义词是"点击齿轮按钮"。类似的还有很多。比如"条目化配置"，它是需求与需求之间的一个关系树，只依附于需求的某个属性上。在需要强化绑定需求之间的关联关系的时候使用，可以在项目卡片的 条目化配置 页面中配置，具体流程如下 巴拉巴拉。
5. 评测驱动：建完不是终点，好用才是。设定目标 → 跑评测 → 分析失败 → 修模型 → 再跑评测。初期是 human in the loop——每一轮都需要人深度介入：看失败 case、判断根因、决定改什么。这个阶段没法跳过，因为失败原因千奇百怪，只有理解业务的人才能判断"这到底是模型没覆盖，还是评测题本身有问题"。当准确率稳定在 85% 以上之后，模式逐渐切换为 human on the loop——系统日常运行不需要人盯着，只有当新增业务场景、用户反馈异常、或准确率出现波动时，人才介入做一轮调优。
6. 「渐进式披露」"短信号 + 长内容"分离：知识库使用yaml格式，每个步骤的格式如下：
```yaml
name: "登录流程"
    synonyms: ["登录", "login", "签入", "认证"]
    category: "脚本范例"
    description: "系统登录的完整自动化脚本，包含用户名密码填写和登录状态验证"
    content: |agent-browser open https://dev-mude-t.tsp.mioffice.cn  | agent-browser wait --load networkidle | ...（完整脚本片段）
```
name（名称）+ synonyms（同义词）→ 用于匹配和重排（短、准、信号强）。description（描述）+ content（正文）→ 只在召回后给 AI 阅读，不参与重排打分。然后三段式检索Rag实现:
```text
@阶段1：混合检索top10,（BM25 + 向量语义）双路检索
def hybrid_search(query: str, entries: list, top_k: int = 10) -> list:
    """双路召回，取并集"""
    # 路线A：BM25 关键词匹配（对 name + synonyms + description 分词后，每个元素单独建索引，包括synonyms）
    bm25_results = bm25_search(query, entries, top_k=top_k)
    
    # 路线B：向量语义匹配（对 name + 每个synonym 单独算embedding相似度）
    vector_results = vector_search(query, entries, top_k=top_k)
    
    # 合并去重
    return merge_results(bm25_results, vector_results)
关键：每个 synonym 单独建索引 + 单独算向量。用户说"成交额"能匹配注册名为"销售额"的条目。
```
```text
@阶段2：去重（按命中来源优先级）
同一条目可能被 name、synonym、description 同时命中
去重规则：name命中 > synonym命中 > description命中
```
```test
@阶段3：重排（只用短文本计算相似度）
阶段 1 为了不漏召回，捞得比较宽松（Top 10）。但直接把 10 个都喂给 LLM 会超 token 上下文，成本高,信息噪音大，LLM 容易被无关内容干扰,所以要再精排一次，只留最相关的 3 个。

方法是去掉10个候选对象的description字段，只用 name + synonyms组成一个对象，把它拼成一个短文本 算作一个向量，参与一次**向量语义检索**，取出3个分最高的对象给ai。因为长描述会把核心信号淹没在背景噪音里。等真正要给 AI 讲清楚"这个字段能不能用、怎么用"时，再把完整的 description 交出去。
```


7. 过程：根据指导老师的提示，我让ai读取excel了解测试步骤，然后Rag匹配知识库，根据提示词和网页的DOM结构生成基础Bash agent-browser脚本，然后再自行调用git-bash工具验证脚本准确性，遇到错误自行修正，直到其改正确。智能体构建完，就算完成pytest化的改造，只需要将输入改成pytest的mark.parametrize。参数，然后打上pytest装饰器即可。最后的CICD步骤也相当复杂，需要用python去构造前后有返回码的新bash脚本，然后让python调bash全部运行，再打成镜像，（详细见思源笔记的 CICD章节），最终部署到Matrix服务器上。智能体包含的组件有duckduckSearch库实现的联网搜索，langgraph实现的内存式全局记忆。

### 核心挑战及解决方案（遇到的问题）：

一是python的subprocess.run 去调用bash运行agent-browser命令，stdout捕获的是 bash 进程的标准输出，而 bash 会把它的输出传递给 agent-browse，这个stdout如果设置为PIPE管道（本意是为了让上层框架调用输出），agent-browser在登录操作的时候会一直卡住，我的解决办法是stdout为文件对象。问题原因是写端（agent-browser）：登录操作持续产生输出，往 pipe 里写，但写满了，需要有人把pipe读掉，清空。读端（Python subprocess.run）：设计上是等进程退出后才一次性读取 pipe，这样导致没人读，就死锁了。二是为了全局保留操作，不能内存保存会话，于是用SqliteSaver来实现文件保存记忆，thread_id会话除了让sqlite自己处理以外，也可以用自带的 python -m sqlite3 tools/excel_to_bash/generator/agent_memory.db工具sqlite使用命令 SELECT DISTINCT thread_id FROM checkpoints;来管理数据库。三是提升精度，还是不能让ai自己改正自己的精度，必须要人为借入，以支持human-in-the-loop中断交互，通过设计一个死循环，循环体执行一个stream流式输出全程，然后通过ai自行调用tool里面的 response = interrupt(data)函数来触发特殊异常GraphInterrupt，LangGraph 捕获这个异常，把问题存入持久化记忆 checkpoint，然后stream 正常结束，后续如果langgraph的agent对象有下一步state，就说明是主动中断，需要用户输入，这里构造即可，然后通过stream_input = Command(resume=user_response) continue。下一个stream继续把stream_input输入，tool里面response就能拿到用户输入，然后返回给ai。如此继续。四是项目初期，参考代码不足，要给ai设计读代码的工具。

### 项目结果

由于agent-browser的如下几个重大缺陷，导致ai生成的bash脚本准确性不足，剥离了流水线的部分，其他部分没再维护了：

1.  Agent-browser的核心命令Snapshot会经常性丢失按钮语义。snapshot 比 html 模式更激进地丢弃了 DOM 中的 CSS 类名、id 等信息，当按钮只有图标没有文字，且缺少 aria-label属性(大多数 Vue/React 项目，在默认情况下几乎不会主动给元素添加 aria-label 属性) 、title 属性、直接文本内容时(作为容器时，没有这些东西很常见)，它在快照中只会被渲染为 generic [ref=eXX] clickable，甚至完全不可见。对 AI agent 而言，这个节点失去了所有功能语义。
2.  Snapshot丢失层级结构导致无法区分两个名字类似的配置按钮。即使使用了语义标签如 &lt;aside&gt;，如果没有上述的aria-label属性、title 属性、直接文本内容时，容器仍会被snapshot丢弃。在我们的开发例子中，aside侧边栏标签里含有侧边栏内容和用户卡片，但里面只有div等元素，但没有直接文本，也没有特殊属性aria-label，快照引擎认为这个父容器节点“没有独立信息价值”，将丢弃它。用户卡片和侧边栏内容都本应是aside侧边栏的子元素，但现在失去了共同的父级，用户卡片（generic）变成了和侧边栏内容的平级，snapshot输出的内容，语义上已经将用户卡片独立于侧边栏之外了，丢失了层级结构。 这个问题，将直接导致无法区分两个名字类似的配置按钮（右上角的空间配置，中间的项目配置按钮）。
3.  Agent-browser自带 HTML 模式，仍无法解决上述问题。Agent-browser get html 命令可以查看选择器内的html，但这导致一次只能获取页面的某个局部区域（如仅 main），丢失了侧边栏、导航栏等关键上下文。如果使用agent-browser get html html获取整个html，就和agent-browser库自己没关系了。
4.  Agent-browser工程化配套缺失，缺乏异常处理、日志等配套能力。纯 shell 环境下函数复用和调试都很困难。虽然可以通过 Python 封装部分缓解，但前两个问题才是制约 agent 可靠性的根本瓶颈。

### 未来的工作
知识库/元数据一旦规模化，就会出现：
1. 有人改了字段名，但忘记更新引用它的地方
2. 同义词配了一半就不管了
3. 描述字段偷懒留空，导致 AI 拿不到上下文

所以可以构建这样的四道防线：

| 防线 | 类比场景 | 本质 |
|------|----------|------|
| 编辑时校验 | 往 entries.yaml 加新条目时，schema 校验 name/synonyms 是否填了 | 静态 lint，写入时拦截 |
| 加载时校验 | 系统启动时检查：有没有 entry 的 category 填了不存在的分类 | 引用完整性检查（类似外键约束） |
| 运行时指纹 | 线上跑的 embedding 缓存 vs 磁盘文件有没有不一致 | 防止"改了源文件但忘了重建索引" |
| 覆盖率看板 | 统计多少条目配了同义词、多少写了描述 | 把质量变成可度量的数字 |


## 第2个项目是一个UI自动化项目，就是刚才agent要替代的对象。

### 项目简介

首先去读本地化的config文件，然后建一个存cookie的文件，然后根据参数(比如-n 10是并行10个浏览器的意思)启动pytest流程，pytest在收集阶段，在conftest.py约定文件的browser_type_launch_args 的约定fixture里面去重写浏览器打开的参数，让其能够并行，然后在每个测试用例都调用一个page_generate的fixtures函数，在page_generate里面去写login登陆逻辑。在login里面，去用config里面的账号密码去登录目标测试网站，然后存到本地的cookie文件中，让并行去共享这个cookie，这里登录的cookie如果config里面写的online变量为True就会带x-ut-run-id键的cookie，没有就只带_aegis_cas键的cookie，这样后端会判断，x-ut-run-id为线上环境，会复制一份数据库和前端，用于线上独立跑测试流水线。然后才执行调用page_generate的fixtures的那个测试用例。

### 遇到的难题

项目中有很多明文密钥，需要加密，这里用的是小米自己开发的服务KeyCenter ，负责管理加解密密钥(cipher key) 的整个生命周期（包括密钥的生成、存储、使用、定期更新，过期淘汰），但并不保存加密后的数据。加密后的密文数据\[encrypted data\]由各业务方自行保存。KeyCenter 独立部署在小米运维团队支持的每个机房（包括c3, c4, aws-de, aws-sgp, 印度, 印尼等）。- 机房间链路出现问题时，不会影响 KeyCenter 服务的可用性。- 国内多个机房之间的 密钥完全相同，加密数据在国内可以跨机房解密。- 国内、海外各区域之间的密钥完全不同，加密数据不可跨区域解密。目前使用的默认加密算法是 AES_128高安全/低CPS 应用版本，是一种对称加密算法，加密和解密操作都只在KeyCenter密钥中心服务的服务器上进行，避免滥用，被强制限制CPS <= 50。相当于加密的时候，我把明文发给keycenter,他用SID对应的密钥 去帮我加密，然后返还给我密文吗。 解密的时候，我把密文发给keycenter，他用SID对应的同一把密钥，帮我解密，返回给我明文。所以密文和明文，都要自己在本地保存，但不放在项目上。KeyCenter服务器采用（ip, sid, password）三项认证KeyCenter使用者。在申请成为新的KeyCenter使用者时，我们会为使用者分配新sid/password对，使用者需要妥善保存该密文并配置在服务器上。不过即使sid/password泄漏，也只有指定的白名单ip才可以访问KeyCenter服务器。

关于AES是一种分组密码，它将明文分成固定大小的块（128位）进行处理。这与流密码（如RC4）形成对比，流密码将数据作为连续的比特流处理。分组密码的设计更复杂，但提供了更强的安全性保证。现代分组密码的核心是替代-置换网络（SPN）结构，它通过多轮操作实现混淆和扩散：混淆：使密钥与密文之间的关系变得尽可能复杂，让攻击者无法从密文中推导出密钥。扩散：将明文中单个比特的影响散布到多个密文比特中，隐藏明文的统计特性。AES的SPN结构比早期的Feistel网络（用于DES）更加高效。Feistel网络每轮只加密一半数据，而SPN每轮加密全部数据，达到相同安全性需要的轮数更少。 SPN结构简化示例： def spn_round(input_block, round_key): """一轮SPN操作：字节替代、行移位、列混合、轮密钥加""" # 字节替代（S盒） - 提供非线性变换 substituted = byte_substitution(input_block) # 行移位 - 提供行内扩散 shifted = shift_rows(substituted) # 列混合（最后一轮省略） - 提供列内扩散 mixed = mix_columns(shifted) # 轮密钥加 - 将密钥引入变换 output = add_round_key(mixed, round_key) return output 现代加密系统不再依赖单一算法或模式，而是构建多层次的安全体系：

- 分层加密策略：
    - 传输层：TLS 1.3（使用AES-GCM）
    - 应用层：对敏感数据单独加密
    - 存储层：磁盘加密（使用AES-XTS）
    - 备份层：使用不同密钥单独加密
- 密钥管理体系：
    - 使用密钥派生函数（如PBKDF2、Argon2）从密码生成密钥
    - 实施密钥轮换策略，定期更新加密密钥
    - 采用密钥层次结构，主密钥保护数据密钥
- 性能与安全平衡：
    - 对大量数据使用GCM模式（支持并行处理）
    - 对短数据使用CBC+HMAC（兼容性更好）
    - 在支持AES-NI的CPU上性能可提升10倍

### 项目影子库设计

我们做 UI 自动化测试时，不能直接污染真实业务数据，所以设计了一套影子库机制。每次自动化测试，登录网站时，请求的cookie原本只有_aegis_cas这个cas验证身份， 现在多一个x-ut-run-id对象，里面的值是UUID，表示这次流水线。然后系统会为这次测试创建一套隔离的数据环境，业务代码基本无感知，只要runid不变，就是用的同一套数据库表。

具体流程就是访问请求带上特殊cookie，端根据这些参数判断当前请求对应的表是否已经复制过。如果没复制，就根据 runId 生成本次测试专属的Mysql影子表名，采用 ut + 日期 + 时间 + 表名缩写 + hash 的规则。如果已经复制过，就直接进入业务逻辑。

业务逻辑真正访问数据库时，会被 MyBatis-Plus 的 InnerInterceptor 拦截，在 SQL 执行前用 JSqlParser 解析 SQL，把里面的逻辑表名替换成本次测试对应的影子表表名，然后更新 BoundSql。这样业务代码还是按原来的表写 SQL，但实际操作的是影子表。中间过程会用redis进行缓存。Mq消息队列去做。

MQ 主要解决的是影子表复制过程比较耗时的问题。复制表结构、复制数据、处理 Redis 缓存和计数器，这些操作如果都放在请求主链路里，会导致接口响应慢，也容易因为并发请求重复触发复制。所以我们把复制任务封装成 MQ 消息，由消费者异步执行，同时配合 Redis 状态或锁保证幂等，避免同一个 runId、同一张表被重复复制。

项目中还解决过一个 Redis 一致性问题。创建文件夹时部分唯一编号依赖 Redis 计数器，如果只复制数据库影子表，不同步 Redis 计数器 value，就会导致新生成的编号和已复制数据冲突，报“数据已存在”。后来我们在复制 Redis 时同时复制 key 和 value，让计数器状态和影子表数据保持一致，解决了这个问题。

### pytest的小经验
1. 由于样例数众多，所以需要考虑pytest-xdist插件来让10个python解释器进程并行，还需要避免进程间的资源冲突 让部分增删改的操作串行执行。所以我们使用了@pytest.mark.xdist_group装饰器来对增删改操作做单独的串行标记，后续让pytest执行贪心算法，进程空闲就把一个执行单元（可以是分好的一组，也可以是单个样例）丢进去，直到下一次进程空闲。
2. 有时候前端按钮不稳定，需要超时之后异常捕捉，然后进行重试的机制，我写了一个函数用于此目的，原本没有。用到了lambda表达式，有两种传法，一种是直接穿函数定义，一种是穿lambda表达式，一定不能穿函数调用 这样会讲函数执行的返回值传入
3. 超时检测机制，用于详细输出超时原因，后面发现是由于元素点击的目标 不太标准，需要点非inheri的标签和外层标签，不能点太里面
4. 资源锁机制，当一个项目名称被修改的时候，另外几个样例需要根据项目名称去查找，会出错。可以利用pytest-xdist自带的filelock 库为每个 reqId(项目id) 创建独立的文件锁，一起挂载到一个物理载体上 即一开始创建的.lock文件，win操作系统回依托这个空文件作为锚点去处理多线程之间的锁互斥。当一个线程的样例 获取到 reqId 后，先获取该 reqId 对应的锁，其它线程中的用例如果需要同一个 reqId，会阻塞等待直到锁被释放。
5. 比较隐蔽的偶发性接口报错问题，可以使用playwright自带的trace view库，在login之前就打开trace view的录制，等样例结束才结束录制。将trace作为zip附件交给allure.attach，它会保存进/reports,后续会被generate给添加到/allure-report里面每个样例的屁股末尾。然后需要查看录制的时候直接找到样例下载附件，然后用playwright show-trace xx.zip命令打开这个网页即可，在里面复现出错时的全部环境，还有每帧的截图。
6. 学会使用jmeter的接口测试工具。要分业务场景设置 事务控制器，然后让所有接口共享一套https的header、 响应时间图、聚合报告 和 查看结果树。如果请求要过域名和网关，就要加上access-sign签名和Cookie进行权限认证，这一步可以通过抓包替你完成，但注意超时时间一般5分钟，可以做简单的迅速的压测。如果要做稳定性测试和复杂压测，则必须绕过域名和网关的权限限制，直连服务器ip和接口，此时就要换成http，并注明端口号，减少header里面专门针对网关和域名的字段。
7. 学会使用jmeter抓包来应对  简单的场景，或者提供一个初始的环境。先在本地启动一个代理服务器，然后在jmeter上新建一个非测试元件 - HTTP(S)测试脚本记录器服务，配置和 代理服务器一样的端口和服务器地址，最后别忘了调整 目标控制器(output)的输出地址为自己这个记录器服务器。此时就可以去网页上刷新看看效果了。

## 第3个项目是小米时期做的ai智能生成报告

### 项目背景：
这个项目是已经做完的项目，我主要负责bug，偶尔增加功能，但问题相当多，因为前前后后有很多人改过。那既然要改bug，而且有些bug比较复杂，就需要去了解这个项目，所以我对这个项目还是比较了解。

### 项目流程：
首先是https://mi.feishu.cn/wiki/DeKhwvf32ivKXgkTFPIcfDYznBf?table=tblxquNEivgb8m3j 这个表格，tblxquNEivgb8m3j表里面是统计数据，另外一张tblPEz86m1eKyafg表里面是具体数据，这两张表的数据每周都有项目开发的人通过机器人报表填好。通过飞书机器人命令或 轮询的定时器触发周报生成后，python会去读取周统计计数和具体的周数据，然后根据表格内容生成图和表格。

其中表格是传统做法，通过当前周和上周计算相应的统计数据，比如这周的total是5，上周的total值是7，就相当于-2的增量，增长率就是-28%，有了数据，直接用python构造echarts表格，然后生成html嵌入，这就是图表的生成。制作内容都会被python打上对应的tag，用于后续的模板替换。

文字的生成则是用的deepseek，根据提前准备好的本周和上周数据，自己构造系统提示词和用户提示词，还有请求的header，用最原始的requests.post()直接请求 DeepSeek API 的 /chat/completions 端点的方式去获取deepseek的回答，没用 LangChain。生成的内容都会被python打上对应的tag，用于后续的模板替换。

最后一步就是飞书block api的相关操作，先提前准备好飞书的模板，然后在模板上准备文本和图片的替换符。前面生成完之后，这里会根据标记一一替换。比如会在模板里面准备'{{text:小标题}}'这样的占位符，如果ai生成内容的tag和占位符的tag一样就替换。具体就是先用飞书api复制一份模板，然后通过block api拿到所有的block，正则匹配遍历拿到所有的占位符，通过飞书api将占位符删掉替换成内容，上传图片，再把这个新文档通过飞书api放到目标节点子节点里面。

#### 部署流程
这个部署和第一个第二个项目不一样，这个没用流水线，是自己部署，自己写一个makefile脚本，在里面的步骤是：先设置几个变量，然后利用本地的dockerfile去build构建打包，-t参数 给本地镜像起一个名字和tag，基于FROM micr.cloud.mioffice.cn/miflow-base/python:3.11镜像包再加上必要的运行环境，这个镜像上传到micr之后，再在服务器界面发布这个镜像即可部署成功。

#### 机器人报表
关于tblPEz86m1eKyafg表里面的数据具体是怎么来的，是通过飞书api构建了一个流程，通过监听"@TSP问题管理 /导入"命令，触发后再调飞书的消息列表 API，get_thread_messages(thread_id) — （/im/v1/messages），用话题 ID 查询该话题下所有消息，取第一条的 message_id，用message_id查询api获取消息的完整内容，将完整内容解析，然后回复一张交互卡片，用户确认后，再根据配置里面的 chat_id 找到对应的 bitable app_token 和 table_id
即目标表tblPEz86m1eKyafg,调飞书 bitable API 写入一条记录

## 第4个项目名字叫洛杉矶犯罪数仓系统。

是计算机设计大赛的参赛作品自选命题，那么这个系统是做什么的呢，就是爬取洛杉矶警察局官网的犯罪数据，再统一整理保存，最后可视化，让人能直观看到犯罪数据的分布、变化和统计结果。因为是我拉的人来，所以我在项目里主要做的是整体框架设计，把各模块任务拆给组员推进，我参考的是当时最常用的hadoop生态，用 Python 爬虫从 LAPD 官网抓取犯罪数据，然后 Springboot做后端，一方面接收爬虫抓到的数据，另一方面给前端提供查询接口，处理完的数据通过缓冲送到Hadoop中进行分布式存储，我们用 Flume + Kafka做数据管道与缓冲，ZooKeeper 集群用来做 Kafka 等组件的协调管理，到此为止数据已经在Hadoop小集群当中，最后再用vue做可视化即可。这个项目是部署在linux上，团队成员均使用git工具保证版本控制。

Vue2使用了axios、pinia、Echarts、elementUI、百度地图API以及一些第三方的css动画组件，打包工具使用的vite。2)后端方面支撑前端并存取数据。springboot用了swagger2、fastjson、jwt、Test单元测试以及用来连接hadoop kafka redis的第三方依赖。 AI智能体方面。后续还测试过在spring中接入deepseek来做智能生成犯罪报告。openai-spring-boot-starter

## 第5个项目是langchain4j和spring vue结合的项目。

其中前端做了基本的request response拦截用于登录，还用了openapi的后端接口文档自动生成前段请求的第三方库。其中后端拥有完整的用户登录模块session、cookie缓存 脱密脱敏，基于AOP的全局异常管理。AI部分使用了langchain4j工厂类集成deepseek，通过声明式的 Ai Service 编程模型快速实现模型调用和网站生成，还用到了rag（embedding模型用的deepseek的text-embedding-3-small，embedding存储形式是直接放在内存中，rag向量化的文本存在本地 是一些优质的前端项目例子）python是用的fastapi顺带完成流式输出，而java则是用的reactor库的flux对象承接的流式输出。 测试方面用junit编写了部分主干功能的测试样例，用jacoco生成了基本的测试报告，根据不同类的覆盖率，提升了代码质量。

# 我的优势

有良好的团队合作意识，能同时以甲方和乙方，多角度的方式来看待一些功能需求的合理性，习惯于和多家单位的合作，具备基本的工作规范和自觉性。同时，我也有一些技术上的经历，做过大量的spring服务器和前端vue的全栈开发，独立开发部署全栈项目，并能够独立进行mysql、mongoDB等 nosql和sql类型数据库的设计和数据提取，也了解一些大数据Hadoop生态的基本操作，区块链智能合约的基本开发，有一点机器学习的相关经验，做过图神经网络的论文撰写。